#include <stdio.h>
#include <string.h>
/**
 * main - prints the program name
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0
 */

string main(char* argv[])
{
printf("%s\n",argv[0]);
return(0);
}
